# Verto v249 — Implementation Report

Session 249 is implemented on top of v248.

Implemented:
- durable Room financial Outbox/Inbox and schema 66→67 migration;
- atomic event creation for invoice create/update/void and payment record/reversal;
- ordered reversal events generated by invoice Void;
- stable event identity, aggregate sequencing across Inbox + Outbox, inbox dedupe, retry/backoff, and dependency waiting;
- financial event sync before legacy invoice/payment row sync;
- explicit lifecycle/version conflict policy with no timestamp LWW for POSTED/VOID financial truth;
- immutable payment financial facts during remote pull;
- `REQUIRES_REVIEW` blocking of legacy invoice/line/payment/inventory/cash paths and preservation of incremental cursors while unresolved;
- additive Supabase event table/RPC contract with tenant isolation, idempotency replay, strict ordering, optimistic conflicts, server revision/time, concurrent-Void protection, and owner-only legacy baseline backfill;
- schema-v1 transition contract documentation and focused F249 verifier.

Verification: F249 focused acceptance verifier passes; F244/F245 migration checks plus F246/F247 checks pass. Historical verifiers that assert exact old Room versions also pass their non-version checks when run temporarily against schema 67. Structural/conflict-marker/sync-slot checks are clean.

Gradle compilation could not start because Gradle 8.9 is not cached and this sandbox has no network access. Supabase SQL is provided but was not executed against a live database here.

See `verification-invoice-F249.md` for details.
