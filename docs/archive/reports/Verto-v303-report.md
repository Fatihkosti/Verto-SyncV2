# Verto v303 — تقرير البناء والتسليم

التاريخ: 2026-08-20

## النتيجة

- البناء: ناجح.
- الأمر المستخدم: `./gradlew assembleRelease --offline --build-cache`
- المدة: 7m 47s.
- المهام: 1255 actionable tasks؛ 765 منفذة، 65 من الكاش، و425 محدثة حتى آخر تشغيل.
- لم يتم تشغيل `clean` ولم يتم حذف أي build أو Gradle cache سابق.
- لم يتم تحديث dependencies أو إصدارات المكتبات، ولم تتغير المعمارية.

## APK

- الملف: `Verto-v303.apk` في جذر المشروع.
- الحجم: 8,002,386 bytes.
- SHA-256: `868ff3789be13092fe3142256f359ad550dc0190f76da25124cea8e97eb63843`.
- الحزمة: `com.verto.app`، الإصدار المعلن `1.0`، وSDK الهدف 35.
- تم فحص سلامة الأرشيف الداخلي بنجاح بواسطة `unzip -tq` والتحقق من التوقيع بواسطة `apksigner`.
- APK هو Release/minified، لكنه موقّع بمفتاح Android debug المحلي لعدم وجود production keystore في نسخة المصدر؛ لذلك يلزم توقيعه بمفتاح الإنتاج قبل النشر العام.

## الاختبارات

- الأمر: `./gradlew test --offline --build-cache`
- النتيجة: ناجح.
- إجمالي تقارير الاختبار: 122 ملفًا، 468 اختبارًا، 0 failures، 0 errors، و0 skipped.
- لم يوجد جهاز أو emulator متصل؛ لذلك لم تُنفذ اختبارات instrumented أو اختبار واجهة على جهاز فعلي.

## الربط وتسجيل الدخول

- تم استخدام عنوان مشروع Supabase الحالي والمفتاح العام للتطبيق عبر متغيرات بيئية مؤقتة فقط، دون كتابتهما في الكود أو Git أو هذا التقرير.
- فحص نقطة Auth للمشروع أعاد HTTP 200.
- لم يُنفذ تسجيل دخول فعلي لعدم تزويد بيانات مستخدم اختبار؛ لذلك لا يمكن إثبات دخول حساب بعينه من بيئة البناء. مسار تسجيل الدخول وتهيئة الجلسة موجودان في المشروع.

## SQL

- لم تُنفذ أي عمليات SQL؛ لم تكن مطلوبة للبناء أو الاختبارات أو فحص Auth.

## الإصلاحات

- إصلاح تعارض أسماء `Context` واستيرادات موارد Android في وحدات الميزات.
- إصلاح استدعاءات الموارد داخل callbacks غير composable.
- إصلاح namespace و`Box` وlabel الخاص بـ`VertoIconButton`.
- تصحيح اختبارات ترحيلات قاعدة البيانات واختبارات التحليلات لتتوافق مع واجهات المشروع ومخطط البيانات الحالي، دون تغيير كود الإنتاج أو إضافة dependencies.

## الأرشيف

- الاسم المطلوب: `Verto-v303.zip`.
- تم إعداد الأرشيف كمصدر فقط مع استبعاد Gradle cache، مجلدات build، ملفات APK/AAB، `local.properties`، ملفات الأسرار، و`google-services.json`.
- تم التحقق من الحجم النهائي، وهو أقل من 20 MiB.
