# Verto v228 — تقرير البناء

التاريخ: 2026-08-17

## النتيجة

- الحالة: PASS
- الأمر: `./gradlew assembleDebug --offline --no-daemon`
- Gradle: 8.9 من Gradle Wrapper والكاش المحلي
- Java: OpenJDK 17.0.19
- المهام: 962 إجمالًا، 918 منفذة، 44 محدثة مسبقًا
- لا توجد تغييرات معمارية أو تغييرات في الاعتماديات.

## APK

- خرج Gradle: `app/build/outputs/apk/debug/app-debug.apk`
- نسخة الجذر: `Verto-v228-debug.apk`
- الحجم: 35,707,598 بايت
- SHA-256: `4711240339de0e0b2eb12d046a91248416030bf93eed7924f8ff4c04c2cdcccf`

## إعدادات البناء

كان البناء يحتاج إلى `google-services.json` مفقودًا. استُخدم إعداد Debug محلي placeholder متوافق مع `com.verto.app` أثناء البناء فقط، ثم حُذف قبل إنشاء حزمة مصدر الحقيقة. لا يحتوي المشروع أو الحزمة على توكن Supabase أو إعداد إنتاج Firebase.

## فحص الخادم وSQL

تم الاتصال بمشروع Supabase `Verto-app` عبر Management API باستخدام التوكن المقدم، دون حفظ التوكن. تم تنفيذ:

- `docs/sql/logistics_v2/009_journey_v3_verification.sql` — قراءة فقط.
- ملخص قراءة فقط للجداول/الأعمدة/الفهارس/RLS.

النتيجة: الجداول الجديدة موجودة، الأعمدة المفقودة `0`، الفهارس المطلوبة المفقودة `0`، RLS مفعّل للجداول الأربع، صلاحية DELETE للمستخدم `authenticated` غير ممنوحة، والجداول القديمة موجودة. هذا الفحص الإداري لا يثبت عزل RLS بين مستخدمين authenticated مختلفين.

## التحذيرات غير المانعة

ظهرت تحذيرات Room عن فهارس مفاتيح خارجية، وتحذيرات Kotlin/Compose عن APIs deprecated أو opt-in. لم تُعدّل هذه العناصر حفاظًا على المعمارية الحالية.

## الحزمة

سيتم إنشاء `Verto-v228.zip` من المصدر مع هذا التقرير، مع استبعاد APK، مجلدات build والكاش، الملفات الحساسة، والسجلات. يجب أن يكون حجم الحزمة أقل من 20 MiB.
